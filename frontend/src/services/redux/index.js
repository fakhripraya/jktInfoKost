import {
    checkAuthenticateUser,
    authenticateUser,
    unauthenticateUser,
    fetchError
} from './main_app_state/actions'
import {
    switchPage
} from './home_state/actions'

export default {
    checkAuthenticateUser,
    authenticateUser,
    unauthenticateUser,
    fetchError,
    switchPage
}